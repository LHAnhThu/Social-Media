<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="10dp">

    <!-- Avatar -->
    <ImageView
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginRight="8dp"
        android:scaleType="centerCrop"
        android:src="@mipmap/user_img"
        android:layout_gravity="center_vertical" /> <!-- Bo tròn avatar -->

    <!-- Nội dung bình luận -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="2dp"
        android:background="#59EFF1F2"
        android:clipToPadding="false"
        android:elevation="2dp"
        android:orientation="vertical"
        android:padding="8dp"
        android:radius="20dp"> <!-- Bo tròn cho phần bình luận -->

        <!-- Chứa tên người dùng và thời gian -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="start|center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/txtUserName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:clipToPadding="false"
                android:radius="8dp"
                android:text="Phạm Ngọc"
                android:textSize="14sp"
                android:textStyle="bold" /> <!-- Bo tròn cho tên người dùng -->

            <TextView
                android:id="@+id/txtCommentTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="3dp"
                android:clipToPadding="false"
                android:radius="8dp"
                android:text="1 giờ"
                android:textColor="#888888"
                android:textSize="12sp" /> <!-- Bo tròn cho thời gian -->
        </LinearLayout>

        <!-- Nội dung bình luận -->
        <TextView
            android:id="@+id/txtCommentContent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:radius="8dp"
            android:text="Đây là bình luận mẫu"
            android:textSize="14sp" /> <!-- Bo tròn cho nội dung bình luận -->

    </LinearLayout>
</LinearLayout>